@model UowAndRepository.Models.Product

@using (Html.BeginForm())
{
<div class="row">
    <div class="form-group ">
        <label>Id</label>
        @Html.TextBoxFor(m => m.Id, new { @class = "form-control"})
    </div>

    <div class="form-group ">
        <label>Name</label>
        @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
    </div>

    <div class="form-group ">
        <label>Unit Price</label>
        @Html.TextBoxFor(m => m.Price, new { @class = "form-control" })
    </div>
    <div class="form-group">
        <button class="btn btn-default" formaction="/Test/Insert">
            <span class="glyphicon glyphicon-star"></span> Insert
        </button>
        <button class="btn btn-default" formaction="/Test/Update">
            <span class="glyphicon glyphicon-star"></span> Update
        </button>
     
    </div>
</div>
}


